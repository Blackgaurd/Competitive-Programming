// memset array with val
#define ms(arr, val) memset(arr, val, sizeof(arr))

// see variable name + value
// useful for debugging
#define watch(x) cout << (#x) << ": " << (x) << '\n'

// when you're too lazy to type 1 extra character
#define nl '\n'
