// LCC/Moose '20 Contest 3 S1 - Peculiar Polling

#include<bits/stdc++.h>
using namespace std;

int n;
char prev = '.';
unordered_map<char, int> mp;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    for (char c='a'; c<='z'; c++) mp.insert({c, 0});
    cin >> n;
    while (n--){
        char a; cin >> a;
        if (a==prev){
            
        }
    }

    return 0;
}