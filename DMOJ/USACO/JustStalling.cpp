// USACO 2021 January Bronze P3 - Just Stalling

#include <bits/stdc++.h>
using namespace std;

int n;
vector<int> cows, stalls;
long long dp(int c, int s) {}
int main() {
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        int a;
        scanf("%d", &a);
        cows.push_back(a);
    }
    for (int i = 0; i < n; i++) {
        int a;
        scanf("%d", &a);
        stalls.push_back(a);
    }
    return 0;
}